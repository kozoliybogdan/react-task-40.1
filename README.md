# Task 40.1 — Controlled / Uncontrolled + Fetch (React + Vite)

Проєкт на React (Vite):
- ControlledForm (useState)
- UncontrolledForm (useRef)
- Запит до API через useEffect (loading / error / success)

## Run locally
```bash
npm install
npm run dev